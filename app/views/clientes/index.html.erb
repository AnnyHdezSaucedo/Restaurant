<h1>Clientes</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Apellido paterno</th>
    <th>Apellido materno</th>
    <th>Correo electronico</th>
    <th></th>
  </tr>

  <% @clientes.each do |clientes| %>
    <tr>
      <td><%= clientes.nombre %></td>
      <td><%= clientes.a_paterno %></td>
      <td><%= clientes.a_materno %></td>
      <td><%= clientes.email %></td>
      <td><%= link_to 'Mostrar', :action => :show, :id => clientes.id %></td>
      <td><%= link_to 'Editar', :action => :edit, :id => clientes.id %></td>
      <td><%= link_to 'Eliminar', {:action => :destroy, :id => clientes.id}, :method => :delete, :data => { :confirm => "¿Eliminar cliente?"} %></td>
    </tr>
  <% end %>
</table>
</br></br><%= link_to 'Crear Cliente', :action => :new %>